{% extends "TTVWebsiteBundle::layout.html.twig" %}

{% block title %}
    Supprimer une figure - {{ parent() }}
{% endblock %}

{% block body %}
    <h3>Supprimer une figure</h3>
    <p>
        Etes-vous certain de vouloir supprimer la figure "{{ trick.name }} ?
    </p>

    <form action="{{ path('ttv_website_delete', {'id' : trick.id}) }}" method="post">
        <a href="{{ path('ttv_website_view', {'id' : trick.id}) }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i> Retour à la figure
        </a>
        <input type="submit" value="Supprimer" class="btn btn-default"/>
        {{ form_rest(form) }}
    </form>
{% endblock %}